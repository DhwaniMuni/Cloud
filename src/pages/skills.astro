---
import BaseLayout from '../layouts/BaseLayout.astro';
import { getWeeks } from '../lib/weeks';
import { getTool, toolSlug, TOOL_CATEGORIES, type ToolCategory } from '../data/tools';
import { weekPath } from '../lib/paths';

/**
 * Skills index — DERIVED at build time from week frontmatter.
 *
 * Nothing here is hand-maintained: add a tool to a week's `tools:` list and it
 * appears here automatically, linked to every week it was used in. Registry
 * entries never referenced by a week are deliberately omitted, so this page
 * reflects what actually happened rather than what is installed.
 */
const weeks = await getWeeks();

interface Usage {
  name: string;
  slug: string;
  docs?: string;
  category: ToolCategory;
  weeks: { week: number; title: string }[];
}

const usageByTool = new Map<string, Usage>();

for (const entry of weeks) {
  for (const name of entry.data.tools) {
    const existing = usageByTool.get(name);
    if (existing) {
      existing.weeks.push({ week: entry.data.week, title: entry.data.title });
      continue;
    }
    const tool = getTool(name);
    usageByTool.set(name, {
      name,
      slug: toolSlug(name),
      docs: tool?.docs,
      // The schema guarantees the tool is registered, so this is never a guess.
      category: tool!.category,
      weeks: [{ week: entry.data.week, title: entry.data.title }],
    });
  }
}

const grouped = TOOL_CATEGORIES.map((category) => ({
  category,
  tools: [...usageByTool.values()]
    .filter((usage) => usage.category === category)
    .sort((a, b) => a.name.localeCompare(b.name)),
})).filter((group) => group.tools.length > 0);

const total = usageByTool.size;
const description = `The ${total} tools and practices used across 8 weeks of the program, grouped by category and linked to the weeks they appeared in.`;
---

<BaseLayout title="Skills" description={description} current="skills">
  <div class="skills">
    <header class="skills-header">
      <h1>Skills</h1>
      <p class="skills-intro">
        {total} tools and practices across {weeks.length} weeks. Every entry is generated from week frontmatter
        — follow a tool through to the weeks where it was used.
      </p>
    </header>

    {
      grouped.map((group) => (
        <section class="category" aria-labelledby={`category-${group.category.toLowerCase()}`}>
          <h2 id={`category-${group.category.toLowerCase()}`} class="category-heading">
            {group.category}
            <span class="category-count">{group.tools.length}</span>
          </h2>

          <ul class="tool-list">
            {group.tools.map((tool) => (
              <li class="tool" id={tool.slug}>
                <h3 class="tool-name">
                  {tool.docs ? (
                    <a href={tool.docs} rel="noopener noreferrer">
                      {tool.name}
                    </a>
                  ) : (
                    tool.name
                  )}
                </h3>
                <ul class="tool-weeks" aria-label={`Weeks that used ${tool.name}`}>
                  {tool.weeks.map((usage) => (
                    <li>
                      <a href={weekPath(usage.week)} class="week-ref" title={usage.title}>
                        Week {usage.week}
                      </a>
                    </li>
                  ))}
                </ul>
              </li>
            ))}
          </ul>
        </section>
      ))
    }
  </div>
</BaseLayout>

<style>
  .skills {
    max-width: var(--content-max);
    margin-inline: auto;
    padding: var(--space-2xl) var(--gutter) 0;
  }

  .skills-header {
    padding-bottom: var(--space-lg);
    border-bottom: var(--border-width) solid var(--border);
  }

  .skills-intro {
    margin-top: var(--space-2xs);
    max-width: var(--measure);
    color: var(--text-secondary);
    line-height: var(--leading-relaxed);
  }

  .category {
    margin-top: var(--space-2xl);
  }

  .category-heading {
    display: flex;
    align-items: baseline;
    gap: var(--space-2xs);
    margin-bottom: var(--space-sm);
    padding-bottom: var(--space-2xs);
    border-bottom: var(--border-width) solid var(--border);
    font-size: var(--text-lg);
  }

  .category-count {
    font-size: var(--text-xs);
    font-weight: var(--weight-normal);
    font-variant-numeric: tabular-nums;
    color: var(--text-muted);
  }

  .tool-list {
    display: grid;
    gap: var(--space-3xs);
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .tool {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-2xs);
    padding: var(--space-2xs) var(--space-xs);
    border: var(--border-width) solid transparent;
    border-radius: var(--radius-sm);
    /* Anchor targets clear the sticky tab bar. */
    scroll-margin-top: calc(var(--tabbar-height) + var(--space-md));
  }

  .tool:hover {
    background-color: var(--bg-subtle);
  }

  /* Highlight the tool a chip linked to, so arriving via #anchor is obvious. */
  .tool:target {
    background-color: var(--accent-subtle);
    border-color: var(--accent-border);
  }

  .tool-name {
    font-size: var(--text-base);
    font-weight: var(--weight-medium);
    letter-spacing: var(--tracking-normal);
  }

  .tool-name a {
    text-decoration: none;
    color: var(--text);
  }

  .tool-name a:hover {
    color: var(--accent);
    text-decoration: underline;
  }

  .tool-weeks {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-3xs);
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .week-ref {
    display: inline-block;
    padding: 0.05rem var(--space-2xs);
    background-color: var(--bg-inset);
    border: var(--border-width) solid var(--border);
    border-radius: var(--radius-pill);
    font-size: var(--text-2xs);
    font-weight: var(--weight-medium);
    color: var(--text-secondary);
    text-decoration: none;
    white-space: nowrap;
  }

  .week-ref:hover {
    color: var(--accent);
    border-color: var(--accent-border);
  }
</style>
