name: Deploy to ECS

on:
  push:
    branches: [develop]
  workflow_dispatch:

# Never run two deploys against the same service at once.
concurrency:
  group: deploy-${{ vars.ECS_SERVICE }}
  cancel-in-progress: false

permissions:
  contents: read

env:
  SOURCE_IMAGE: nginxdemos/hello:plain-text
  # The Fargate tasks run on ARM64; GitHub runners are amd64, so pull explicitly.
  PLATFORM: linux/arm64
  CONTAINER_NAME: app

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v6
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ vars.AWS_REGION }}

      - name: Log in to Amazon ECR
        id: ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Pull public image
        run: docker pull --platform "$PLATFORM" "$SOURCE_IMAGE"

      - name: Tag and push image to ECR
        id: image
        env:
          # The repository is IMMUTABLE, so every push needs a unique tag.
          IMAGE_URI: ${{ steps.ecr.outputs.registry }}/${{ vars.ECR_REPOSITORY }}:${{ github.sha }}
        run: |
          docker tag "$SOURCE_IMAGE" "$IMAGE_URI"
          docker push "$IMAGE_URI"
          echo "uri=$IMAGE_URI" >> "$GITHUB_OUTPUT"

      - name: Download current task definition
        run: |
          TASK_DEF_ARN=$(aws ecs describe-services \
            --cluster "${{ vars.ECS_CLUSTER }}" \
            --services "${{ vars.ECS_SERVICE }}" \
            --query 'services[0].taskDefinition' --output text)
          aws ecs describe-task-definition \
            --task-definition "$TASK_DEF_ARN" \
            --query taskDefinition > task-definition.json

      - name: Render task definition with new image
        id: render
        uses: aws-actions/amazon-ecs-render-task-definition@v1
        with:
          task-definition: task-definition.json
          container-name: ${{ env.CONTAINER_NAME }}
          image: ${{ steps.image.outputs.uri }}

      - name: Deploy to ECS and wait for stability
        uses: aws-actions/amazon-ecs-deploy-task-definition@v2
        with:
          task-definition: ${{ steps.render.outputs.task-definition }}
          cluster: ${{ vars.ECS_CLUSTER }}
          service: ${{ vars.ECS_SERVICE }}
          wait-for-service-stability: true
          wait-for-minutes: 10

      - name: Verify public endpoint
        run: |
          for i in $(seq 1 10); do
            if curl -fsS --max-time 10 "${{ vars.ALB_URL }}"; then
              echo "Endpoint healthy"
              exit 0
            fi
            echo "Attempt $i failed, retrying in 10s..."
            sleep 10
          done
          echo "Endpoint did not respond successfully" >&2
          exit 1
